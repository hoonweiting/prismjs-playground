<pre class="language-javascript"><code>// keyword, operator, string, punctuation, number
var foo = 'bar', baz = 5;

// boolean, function, template-string
if (true) &#123;
  console.log(`foo $&#123;foo&#125;`);
  doSomething('silly');
&#125;

// regex
let regex = /\/\*[\w\W]*?\*\//g;</code></pre>
